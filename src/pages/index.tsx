import * as React from "react";
import { MainApp } from '../components/core/MainApp';

// markup
const IndexPage = () => {
  return (
    <main>
      <MainApp />
    </main>
  )
}

export default IndexPage;
